name: flow
description: "A new Dart package project."
version: 0.0.1
homepage: https://github.com/Moniepoint/dart-flow

environment:
  sdk: ">=3.0.0 <4.0.0"

dev_dependencies:
  lints: ^3.0.0
  test: ^1.24.0
